describe("saskavi ", function() {

  it('should be able to call a remote function', function() {
    expect(true).to.be.false;
  });

  it('should invoke the callback with the return value of the remote function', function() {
    expect(true).to.be.false;
  });

  it('should be able to call functions with no parameters', function() {
    expect(true).to.be.false;
  });


});